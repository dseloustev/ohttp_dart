## Unreleased

- AW-2865 Phase 1: Read-only RFC 9180 audit of `lib/src/hpke.dart`. All labeled-KDF, KEM Encap, key-schedule, nonce-derivation, and export sections verified correct. Seven findings produced (2 HIGH, 5 IMPROVEMENT, 0 BLOCKER); draft Jira tasks TASK-1 through TASK-7 recorded. No code changed.
- AW-2865 Phase 2: Read-only RFC 9292 audit of `lib/src/bhttp.dart`. Framing indicator guard verified correct (positive verdict). Six gaps found (all HIGH): status code not range-validated, header loop has no max-count/max-size guard, body sublist throws RangeError on truncated input, decodeVarint has no buffer-length guards for multi-byte widths, serializeRequest has no body size upper bound, no negative tests for adversarial input. One IMPROVEMENT: 8-byte varint untested. Draft Jira tasks TASK-B1 through TASK-B7 recorded. No code changed.
- AW-2865 Phase 3: Read-only RFC 9458 audit of `lib/src/ohttp.dart`. Four positive verdicts (KeyConfig wire layout, HPKE info string, plain-HKDF response decap, response empty AAD). Four HIGH findings: silent drop of extra KDF+AEAD pairs when symLen > 4 (O-2); SecretBoxAuthenticationError leaks package:cryptography into public API (O-9); enc and exportedSecret not zeroized in OhttpEncapsulateResult (O-10); test suite missing round-trip, auth-failure, multi-suite, and truncated-ciphertext tests (O-11). Two IMPROVEMENT findings: exception-type inconsistency between parse() and validate() (O-4); empty-AAD deviation needs inline comment (O-6). One MAINTENANCE finding: plain-HKDF call sites in ohttpDecapsulate must not be unified with labeled variants (O-7). Notable: vision.md §5.1 predicted a RangeError risk on short KeyConfig buffer; audit confirmed this risk does NOT exist in the current code (O-3 positive). Draft Jira tasks TASK-O1 through TASK-O7 recorded. No code changed.
- AW-2865 Phase 4: Read-only audit of `lib/src/ohttp_client.dart` (the only layer a wallet integrator calls). Twelve findings produced (9 HIGH, 3 IMPROVEMENT, 0 BLOCKER); draft Jira tasks TASK-C1 through TASK-C12 recorded. Key gaps: no HTTPS-scheme enforcement on gateway URL (C-1), no timeout or caching on KeyConfig GET (C-5), no timeout or cancellation on gateway POST (C-6), sendDirect() bypasses OHTTP with no privacy warning (C-4), targetAuthority embedded verbatim with no allow-list (C-3), no typed exception hierarchy (C-7/C-8), no response body size cap creating two-layer DoS with Phase 2 R-2 (C-9). One cross-layer confirmation: SecretBoxAuthenticationError reaches wallet caller unwrapped (C-11, resolved by Phase 3 TASK-O5). No code changed.

## 0.1.0

- OHTTP client (RFC 9458) — encapsulate/decapsulate HTTP requests via gateway
- HPKE Base Mode Sender (RFC 9180) — pure Dart, tested against RFC test vectors
- Binary HTTP (RFC 9292) — serialize/parse HTTP messages
- High-level `OhttpClient` with configurable gateway
- Tested on iOS, macOS, Android, Windows
